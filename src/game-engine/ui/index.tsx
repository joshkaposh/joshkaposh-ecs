import type { Component } from "solid-js";
import App, { AppStates, AppEvents } from "..";

// const UI: Component = (props) => {

//     return 
// }

function create_ui(app: App, options: {
    ui: Component;
    event: string;
}) {
    // app.get_event(AppEvents, '')
    // return 
}